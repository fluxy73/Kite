name: kite
description: Kite — messagerie premium (Flutter, client du serveur Go).
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  crypto: ^3.0.7
  flutter_contacts: ^1.1.9
  flutter_local_notifications: ^22.3.0
  flutter_webrtc: ^0.12.4
  just_audio: ^0.10.0
  path_provider: ^2.1.4
  record: ^7.1.1
  local_auth: ^3.0.2
  local_auth_android: ^2.0.10

# path_provider_foundation >=2.5 uses native-assets build hooks (objective_c),
# qui échouent avec un chemin de SDK contenant des espaces sur Windows.
# 2.4.x reste sur les method channels (aucun hook).
dependency_overrides:
  path_provider_foundation: 2.4.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
